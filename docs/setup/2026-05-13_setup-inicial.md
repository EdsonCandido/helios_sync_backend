# Setup Inicial do Projeto

## Objetivo
Configurar repositório inicial, adicionar regras de ignorar arquivos e preparar o projeto backend.

## Alterações
- Criação do arquivo `.gitignore` para ignorar `node_modules`, arquivos de build e variáveis de ambiente.
- Adição dos arquivos de configuração iniciais (`package.json`, `tsconfig.json`, `biome.json`).

## Endpoints afetados (se houver)
- Nenhum.

## Regras de negócio
- Manter `.env` e arquivos temporários fora do controle de versão.

## Observações
- Nenhum ponto adicional.
